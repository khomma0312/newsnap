# ニュースクリップ＋要約アプリ 仕様ドキュメント

## 概要

NewsAPI でニュースを探索・クリップし、AWS Bedrock で自動要約を生成するアプリ。  
認証は Amazon Cognito（Hosted UI）で行い、ログインユーザーのみ利用可能。

インフラ構成（AWS・Terraform）は別リポジトリ（`newsnap-infra`）の `infra-spec.md` を参照。

### 技術スタック

| レイヤー | 技術 |
|---|---|
| フロントエンド | Next.js（Static Export）|
| バックエンド | Hono（Node.js）|
| 認証 | Amazon Cognito（Hosted UI）|
| 外部API | NewsAPI、AWS Bedrock（Claude 3 Haiku）|

---

## 機能一覧

### 認証

| 機能 | 概要 |
|---|---|
| ログイン | Cognito Hosted UI へリダイレクト、認証後にフロントへコールバック |
| ログアウト | Cognito セッションを破棄してトップへリダイレクト |
| 認証保護 | 未ログイン状態でのアクセスはログイン画面へリダイレクト |
| JWT検証 | Hono ミドルウェアで Cognito の JWT 署名を検証 |

### 記事管理（CRUD）

| 機能 | 概要 |
|---|---|
| 記事一覧表示 | 保存済み記事をタイトル・要約・タグ・登録日で一覧表示 |
| 記事詳細表示 | 要約・タグ・元記事リンクを表示 |
| 記事登録（URLから） | URL を入力すると OGP 情報（タイトル・サムネイル）を自動取得して保存 |
| 記事登録（NewsAPIから） | ニュース探索画面からワンクリックで保存 |
| 記事削除 | 保存済み記事を削除 |
| タグ登録・編集・削除 | 記事に紐づくタグを管理 |

### AI 要約（Bedrock 連携）

| 機能 | 概要 |
|---|---|
| 自動要約生成 | 記事登録時に Bedrock（Claude 3 Haiku）で 3〜5 行の要約を自動生成 |
| 要約の再生成 | 記事詳細画面から手動で要約を再生成 |

### ニュース探索（NewsAPI 連携）

| 機能 | 概要 |
|---|---|
| ニュース検索 | キーワード・カテゴリを指定して NewsAPI からニュース一覧を取得 |
| 記事の保存 | 探索結果からワンクリックでクリップに追加 |

### 検索・フィルタ

| 機能 | 概要 |
|---|---|
| タグ絞り込み | タグを選択して記事一覧をフィルタ |
| キーワード検索 | タイトル・要約に対してキーワード検索 |

---

## 画面構成

```
/                    トップ（保存済み記事一覧 + 検索・フィルタ）
/articles/:id        記事詳細（要約・タグ・元記事リンク・要約再生成ボタン）
/explore             ニュース探索（NewsAPI 検索・カテゴリ選択・保存ボタン）
/tags                タグ管理（一覧・登録・編集・削除）
/callback            Cognito 認証後のコールバック処理（画面なし）
```

---

## 画面詳細

### / トップ（記事一覧）

- 保存済み記事をカード形式で一覧表示
- 各カード：サムネイル・タイトル・要約（先頭 2 行）・タグ・登録日
- タグフィルタ（複数選択可）
- キーワード検索バー
- 「ニュースを探す」ボタン → `/explore` へ遷移

### /articles/:id 記事詳細

- タイトル・サムネイル・要約全文・タグ一覧
- 元記事を開くリンク
- 要約を再生成するボタン（Bedrock 再呼び出し）
- タグの追加・削除
- 記事の削除ボタン

### /explore ニュース探索

- キーワード入力 + カテゴリ選択（technology / business / health 等）
- NewsAPI から取得したニュース一覧をカード形式で表示
- 各カード：タイトル・サムネイル・メディア名・配信日・「保存」ボタン
- 保存済みの記事にはチェックマーク表示

### /tags タグ管理

- タグ一覧（名前・紐づく記事数）
- タグの新規登録フォーム
- タグの編集・削除

---

## DBスキーマ（概要）

```
articles
  id            UUID
  user_id       STRING（Cognito sub）
  url           TEXT
  title         TEXT
  thumbnail_url TEXT
  summary       TEXT（Bedrock 生成）
  created_at    TIMESTAMP

tags
  id            UUID
  user_id       STRING
  name          STRING

article_tags
  article_id    UUID（FK）
  tag_id        UUID（FK）
```

DDL の詳細は `backend/src/db/schema.sql` を参照。

---

## 意図的に省く機能

| 機能 | 省く理由 |
|---|---|
| 記事本文の編集 | URL と要約が主体のため編集ユースケースが薄い |
| お気に入り・既読管理 | PoCスコープ超え |
| Bedrockによるレコメンド | 要約のみで十分差別化できる、RAG は工数大 |
| マルチユーザー管理画面 | インフラの複雑さが増す割にポートフォリオの主役ではない |
| ソーシャルログイン（Google等） | Cognito 単体で認証の仕組みは示せる |
